# Brier Score Predictor

A local Windows desktop app for logging binary probabilistic predictions, resolving outcomes, and tracking Brier score plus calibration.

## Run

```bat
npm start
```

## Build single executable package

```bat
build-windows.cmd
```

The packaged app is written to `release`.

For an offline folder build, run:

```bat
npm.cmd run build:folder
```
